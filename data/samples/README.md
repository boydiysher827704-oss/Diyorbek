# data/samples

100 randomly selected training images from the dataset, included here as
a quick visual reference (so a reviewer can browse real examples without
downloading the full dataset).

This is a sample, not the full dataset. For the complete dataset and
exact download instructions, see DATASET.md in the repository root.
