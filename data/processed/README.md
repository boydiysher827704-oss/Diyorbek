# data/processed

Cleaned, split, or otherwise prepared data goes here (train/val/test
splits, resized images, normalized labels).

This data should always be reproducible from data/raw/ by running a
script in src/ - never edited by hand.
